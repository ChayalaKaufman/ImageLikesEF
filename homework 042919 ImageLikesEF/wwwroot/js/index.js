$(() => {
    //i tried to do it here, but i realized i don't know how to do for multiple images at a time...
    //setInterval(() => {
    //    const id = $(".like").data('id')
    //    $.get("/home/getLikes", { id: id }, function (likes) {
    //        $(".likes").text(likes);
    //    })
    //}, 1000)
})